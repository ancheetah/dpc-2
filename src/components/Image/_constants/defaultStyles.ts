// this is the only component with default styles since we are using builder's built in Image component.

export const defaultStyles = {
   position: 'relative',
   width: '100%',
   minHeight: '20px',
   minWidth: '20px',
   overflow: 'hidden',
};
